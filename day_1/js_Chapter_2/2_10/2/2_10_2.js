"use strict";
let str = prompt('What is the “official” name of JavaScript?',);

if(str == 'ECMAScript')
    {
        alert('Right!');
    }
else
    {
        alert("You don't know? ECMAScript!");
    }